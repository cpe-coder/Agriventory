<Window x:Class="Agriventory.View.CashierDashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:menuBar="clr-namespace:Agriventory.Utils"
        xmlns:viewModel="clr-namespace:Agriventory.ViewModel"
        xmlns:view="clr-namespace:Agriventory.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        MouseDown="DashboardView_OnMouseDown"
        Title="Dashboard" 
        MinHeight="650" 
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        AllowsTransparency="True"
        Icon="/Assets/logo.ico"
        MinWidth="1000">
    
    <Window.DataContext>
        <viewModel:CashierNavigationViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:HomeViewModel}">
            <view:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:ChickenViewModel}">
            <view:ChickenView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:PigViewModel}">
            <view:PigView/>
        </DataTemplate>
      
        <DataTemplate DataType="{x:Type viewModel:StartupViewModel}">
            <view:StartupView></view:StartupView>
        </DataTemplate>
    </Window.Resources>
   
      <Border  CornerRadius="12" 
            MouseDown="Background_MouseDown"
            MouseLeftButtonDown="OnMouseLeftButtonDown"
            BorderBrush="#ff5757"
            BorderThickness="1"
            Background="#161622"
    >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
         
            <Grid Grid.Column="0" Background="Transparent">
                <Border Background="#222236"
                        CornerRadius="12,0,0,12" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="95"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                  CornerRadius="0,0,10,0"
                                  BorderBrush="#161622"
                                  Padding="0,0,0,15"
                                  BorderThickness="0,0,0,5">
                            <Image Source="/Assets/dashboardLogo.png"
                                   Height="50"
                                   Margin="0,20,0,0"/>
                        </Border>
                        <StackPanel Grid.Row="1">
                          <ItemsControl ItemsSource="{Binding SourceCollection}">
                              <ItemsControl.ItemTemplate>
                                  <DataTemplate>
                                      <StackPanel>
                                          <menuBar:Btn 
                                                       GroupName="MenuGroup"
                                                       CommandParameter="{Binding MenuName}"
                                                       Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}, Path=DataContext.MenuCommand}"
                                                       Style="{StaticResource BtnStyle }">
                                              <Grid >
                                                  <Image Source="{Binding MenuImage}" Style="{StaticResource ImageStyle}" />
                                                  <TextBlock Text="{Binding MenuName}"
                                                             Style="{StaticResource TextStyle}" />

                                              </Grid>
                                          </menuBar:Btn>
                                      </StackPanel>
                                  </DataTemplate>
                              </ItemsControl.ItemTemplate>
                          </ItemsControl>
                        </StackPanel>
                        <StackPanel Grid.Row="2">
                            <Border CornerRadius="0,10,0,0"
                                    
                                    BorderBrush="#161622"
                                    BorderThickness="0,5,0,0" 
                                    Padding="0,0,0,10">
                              <menuBar:Btn Click="ButtonBase_OnClick"
                                  Style="{StaticResource LogoutButtonStyle}" >
                                  <Grid>
                                      <Image Source="/Assets/logout.png" Style="{StaticResource ImageStyle}"/>
                                      <TextBlock Text="Log Out" Style="{StaticResource TextStyle}" />
                                  </Grid>
                              </menuBar:Btn>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
              <Grid Grid.Column="1">
                  <Grid.RowDefinitions>
                      <RowDefinition Height="60"/>
                      <RowDefinition Height="*"/>
                  </Grid.RowDefinitions>
                  <Border Grid.Row="0"
                          CornerRadius="0,12,10,10"
                          Background="#222236"
                          Margin="2,0,00,0">
                      <Grid Background="Transparent" >
                         <Grid.ColumnDefinitions>
                             <ColumnDefinition/>
                             <ColumnDefinition/>
                         </Grid.ColumnDefinitions>
                          <StackPanel Grid.Column="1"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Center">
                              
                              <Image Margin="0,0,20,0" 
                                     Source="/Assets/avatar.png" Height="35" />
                          </StackPanel>
                      </Grid>
                  </Border>
                  <StackPanel Grid.Row="1">
                      <ContentControl  x:Name="Pages"
                                      Content="{Binding SelectedViewModel}"/>
                  </StackPanel>
              </Grid>
        </Grid>
    </Border>
    
</Window>
