<UserControl x:Class="Agriventory.View.TransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">

    
   
    <Grid Margin="20">
        
        <StackPanel>

            <StackPanel Grid.Row="0" Margin="0,0,0,10" >
                <TextBlock Text="Transaction History" Foreground="#ff5757" Margin="0,5,0,0" FontSize="30" FontWeight="Bold"/>
            </StackPanel>
            
            <DataGrid x:Name="TransactionDataGrid"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="40"/>
                    <DataGridTextColumn Header="Names" Binding="{Binding Names}" Width="*"/>
                    <DataGridTextColumn Header="Products" Binding="{Binding Product}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                    <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="150"/>
                    <DataGridTextColumn Header="Date Delivered" Binding="{Binding DateDelivered}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>
    </Grid>
</UserControl>
