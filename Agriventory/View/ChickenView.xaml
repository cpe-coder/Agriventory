<UserControl x:Class="Agriventory.View.ChickenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Chicken Feed Management" Style="{StaticResource SectionHeader}" />

        <Border Grid.Row="1" Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Add/Edit Form -->
                <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                    <TextBox Width="200" Text="hello" Margin="5"/>
                    <TextBox Width="100" Margin="5" />
                    <Button Content="Add" Background="{StaticResource PrimaryColor}" Foreground="White" Width="80" Margin="5"/>
                    <Button Content="Edit" Background="#444" Foreground="White" Width="80" Margin="5"/>
                    <Button Content="Delete" Background="#333" Foreground="White" Width="80" Margin="5"/>
                </StackPanel>

                <!-- Data Grid -->
                <DataGrid Grid.Row="1"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          HeadersVisibility="Column"
                          Margin="0,10,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Feed Name" Binding="{Binding FeedName}"/>
                        <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
